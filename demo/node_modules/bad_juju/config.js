const config = {
  QA_MIGHT_BE_WORKING: false,
  HAVE_SENT: false,
  RANDOMIZE: false,
  API_DOMAIN: 'http://localhost:8000/', // ideally, this would be a domain that looks like an analytics network
};

export { config };
